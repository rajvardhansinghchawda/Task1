service: email-service
frameworkVersion: "3"

provider:
  name: aws
  runtime: python3.9

functions:
  sendEmail:
    handler: handler.send_email
    events:
      - httpApi:
          path: /send-email
          method: post

plugins:
  - serverless-offline
